---
import {Image} from "astro:assets"




interface Props {
path: string;
width: number ;
height: number;
alt: string;
}

const {path, width, alt , height, } = Astro.props
---

<Image src={path} width={width} alt={alt} height={height}  class="w-full h-80 object-medium object-cover"/>

